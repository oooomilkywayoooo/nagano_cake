<!DOCTYPE html>
<html>
  <head>
    <title>NaganoCake</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="/"><h1><strong>Nagano Cake</strong></h1></a>
        <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span></button>
        <div class="navbar-collapse collapse" id="navbarNavDropdown" style>
          <ul class="navbar-nav ml-auto">
            <% if admin_signed_in? %>
              <li><%= link_to '商品一覧', admin_items_path, class: 'd-{inline-block} mr-5 px-3 nav-link text-light btn btn-secondary'%></li>
              <li><%= link_to '会員一覧', "#", class: 'd-{inline-block} mr-5 px-3 nav-link text-light btn btn-secondary'%></li>
              <li><%= link_to '注文履歴一覧', "#", class: 'd-{inline-block} mr-5 px-3 nav-link text-light btn btn-secondary'%></li>
              <li><%= link_to 'ジャンル一覧', "#", class: 'd-{inline-block} mr-5 px-3 nav-link text-light btn btn-secondary'%></li>
              <li><%= link_to 'ログアウト', destroy_admin_session_path, method: :delete, class: 'd-{inline-block} mr-5 px-3 nav-link text-light btn btn-secondary'%></li>
            <% else %>
              <li><%= link_to 'About', "/about", class: 'd-{inline-block} mr-5 px-3 nav-link text-light btn btn-secondary'%></li>
              <li><%= link_to '商品一覧', admin_items_path, class: 'd-{inline-block} mr-5 px-3 nav-link text-light btn btn-secondary'%></li>
              <li><%= link_to '新規登録', new_customer_registration_path, class: 'd-{inline-block} mr-5 px-3 nav-link text-light btn btn-secondary'%></li>
              <li><%= link_to 'ログイン', customer_session_path, class: 'd-{inline-block} mr-5 px-3 nav-link text-light btn btn-secondary'%></li>
            <% end %>
          </ul><br>
          <form class="navbar-form">
            <div class="form-group">
              <input type="text" class="form-control" placeholder="Search"></input>
            </div>
            <button type="submit" class="btn btn-info">検索</button>
          </form>
        </div>
      </div>
    </nav>
  </header>

  <body>
    <%= yield %>
  </body>
</html>
