<div class="container mx-auto">
  <p class="mt-5">
    <h4 class="mb-5"><strong class="bg-light px-3">商品詳細</strong></h4>
  </p>
  <div class="row">
    <div class="col-lg-4">
      <% if @item.image.attached? %>
        <%= image_tag @item.image, size: "300x220" %>
      <% else %>
        <%= image_tag 'no_image1', size: "300x220" %>
      <% end %>
    </div>
    <div class="col-lg-3">
      <p>商品名</p>
      <p>商品説明</p>
      <p>ジャンル</p>
      <p>税込価格（税抜価格）</p>
      <p>販売ステータス</p>
    </div>
      <div class="col-lg-5">
        <p><%= @item.name %></p>
        <p><%= @item.introduction %></p>
        <p><%= @item.genre.name %></p>
        <p><%= @item.add_tax_price.to_s(:delimited) %>(<%= @item.price %>)円</p>
        <p><% if @item.is_active == true %>
          販売中
        <% elsif @item.is_active == false %>
          販売停止中
        <% end %></p>
      </div>
    </div>
    <div class="text-center mt-3"><%= link_to '編集する', edit_admin_item_path(@item), class: "btn btn-success px4" %></div>
  </div>
</div>