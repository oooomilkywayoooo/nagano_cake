<div class="container mx-auto">
  <p class="mt-5">
    <h4 class="mb-5"><strong class="bg-light px-3">商品詳細</strong></h4>
  </p>
  <div class="row">
    <div class="col-lg-4">
      <% if item.image.attached? %>
        <%= image_tag item.image, aize:"200x200" %>
      <% else %>
        <%= image_tag 'no_image1', size:'200x200' %>
      <% end %>
    </div>
    <div class="col-lg-8">
      <p>商品名</p>
      <div><%= @item.name %></div>
      <p>商品説明</p>
      <div><%= @item.introduction %></div>
      <p>ジャンル</p>
      <div><%= @item.genre_id %></div>
      <p>税込価格（税抜価格）</p>
      <div><%= @product.add_tax_price.to_s(:delimited) %>(<%= @item.price %>)円</div>
      <p>販売ステータス</p>
      <div><%= @item.is_active %></div>
    </div>
    <div class="row">
      <div class="text-center"><%= f.submit '編集する', class: "btn btn-success px4" %></div>
    </div>
  </div>
</div>